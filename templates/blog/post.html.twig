{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{%  block body %}

    <p>ID {{ id }}</p>
    <h3>
        {{ post.title|upper }}
    </h3>
    <p>{{ post.text }}</p>
    <p>{{ post.date|date('H:i:s') }}</p>
    {{ '24536'|price }}
    <p>And the locale is {{ locale }}</p>
    <p>{{ message }}</p>

    <p>{{ app.environment }}</p>
    <p>{{ app.user.username }}</p>
    <p>{{ app.request }}</p>
    <p>{{ app.session.isStarted() }}</p>
{% endblock %}